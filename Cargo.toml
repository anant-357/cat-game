[package]
name = "cat_game"
version = "0.1.0"
edition = "2024"

[features]
debug = []

[dependencies]
bevy = { version = "0.17.2", features = ["experimental_bevy_ui_widgets"] }
serde = "1.0.219"
strum = { version = "0.27.1", features = ["derive"] }
